llm:
  provider: ${LLM_PROVIDER}
  api_key: ${LLM_API_KEY}
  model: ${LLM_MODEL}
mcp:
  playwright:
    url: http://localhost:8000
  filesystem:
    url: http://localhost:8001
  notion:
    url: http://localhost:8002
